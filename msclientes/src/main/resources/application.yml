spring:
  application:
    name: msclientes

server:
  port: 0 #Isso significa q é uma porta aleatória

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    instance:
      instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
